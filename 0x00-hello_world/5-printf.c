#include <stdio.h>

/**
 *main - prints a specified string
 *Return: 0
*/
int main(void)
{
	char str = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
